default: &default
  adapter:  postgresql
  host:     localhost
  encoding: unicode
  pool:     20
  template: template0
  port: 5432
  password: postgres
  username: postgres

development:
  <<: *default
  database: postgres

test:
  <<: *default
  database: test

production:
  <<: *default
  database: production